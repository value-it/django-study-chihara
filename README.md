# django-study-chihara
Dockerを使ったDjangoの学習  

# 実行手順

1. Django-Env-SetUpをクローンする
1. クローン先へディレクトリへ移動する
1. `docker-compose` コマンドを実行する
1. コンテナ実行後、Docker上に立っているDjangoのページにアクセスできることを確認する
[http://localhost:8000/](http://localhost:8000/) 


